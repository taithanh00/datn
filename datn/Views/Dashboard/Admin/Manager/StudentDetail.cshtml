@model datn.Models.Student
@{
    Layout = "_DashboardLayout";
    var backUrl = ViewBag.BackUrl as string ?? "/Manager/Students";
    var backText = ViewBag.BackText as string ?? "Quay lại danh sách";
}

<div class="page-header">
    <div class="header-left">
        <div class="header-icon">
            <i class="fa-solid fa-user-graduate"></i>
        </div>
        <div>
            <h2 class="page-title">HÃ´Ì€ sÆ¡ hoÌ£c sinh</h2>
            <p class="page-subtitle">Quáº£n lÃ½ thÃ´ng tin chi tiáº¿t cá»§a há»c sinh</p>
        </div>
    </div>
    <div class="header-actions">
        <a href="@backUrl" class="btn btn-secondary">
            <i class="fa-solid fa-arrow-left"></i> @backText
        </a>
    </div>
</div>

<div class="student-profile-grid">
    <!-- Left Column: Avatar & Basic Stats -->
    <div class="profile-sidebar">
        <div class="card profile-card">
            <div class="avatar-wrapper">
                <img src="@(Model.AvatarPath ?? "/images/lion_orange.png")" alt="Avatar" />
                <div class="status-indicator active"></div>
            </div>
            <h3 class="student-name">@Model.FullName</h3>
            <span class="student-code">ID: @Model.Id</span>
            
            <div class="quick-stats">
                <div class="stat-item">
                    <span class="stat-label" style="display: flex; flex-direction: column; justify-content: center; align-items: center;">Lá»›p</span>
                    <span class="stat-value">@(Model.Class?.Name ?? "N/A")</span>
                </div>
                <div class="stat-item">
                    <span class="stat-label" style="display: flex; flex-direction: column; justify-content: center; align-items: center;">Giá»›i tÃ­nh</span>
                    <span class="stat-value">@(Model.Gender ? "Nam" : "Ná»¯")</span>
                </div>
            </div>
        </div>

        <div class="card info-card mt-4">
            <h4>ThÃ´ng tin liÃªn hÃªÌ£</h4>
            <div class="info-list">
                <div class="info-item">
                    <i class="fa-solid fa-location-dot"></i>
                    <div>
                        <label>Äá»‹a chá»‰</label>
                        <p>@(Model.Address ?? "ChÆ°a cáº­p nháº­t")</p>
                    </div>
                </div>
                <div class="info-item">
                    <i class="fa-solid fa-calendar-day"></i>
                    <div>
                        <label>NgÃ y nháº­p há»c</label>
                        <p>@(Model.EnrollDate?.ToString("dd/MM/yyyy") ?? "N/A")</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Right Column: Detailed Info & Tabs -->
    <div class="profile-main">
        <div class="card main-card">
            <div class="card-header-tabs">
                <button class="tab-btn active" data-tab="basic">ThÃ´ng tin cÃ¡ nhÃ¢n</button>
                <button class="tab-btn" data-tab="parents">Phá»¥ huynh</button>
                <button class="tab-btn" data-tab="history">Lá»‹ch sá»­ há»c táº­p</button>
            </div>
            
            <div class="tab-content active" id="basic">
                <div class="detail-grid">
                    <div class="detail-group">
                        <label>Há»</label>
                        <p>@Model.LastName</p>
                    </div>
                    <div class="detail-group">
                        <label>TÃªn</label>
                        <p>@Model.FirstName</p>
                    </div>
                    <div class="detail-group">
                        <label>NgÃ y sinh</label>
                        <p>@Model.DateOfBirth.ToString("dd/MM/yyyy")</p>
                    </div>
                    <div class="detail-group">
                        <label>Giá»›i tÃ­nh</label>
                        <p>@(Model.Gender ? "Nam" : "Ná»¯")</p>
                    </div>

                    @{
                        var father = Model.ParentStudents?
                            .FirstOrDefault(ps => ps.Relationship == "Bá»‘")?.Parent;
                        var mother = Model.ParentStudents?
                            .FirstOrDefault(ps => ps.Relationship == "Máº¹")?.Parent;
                    }

                    @if (father != null)
                    {
                        <div class="detail-group">
                            <label>Há» tÃªn bá»‘</label>
                            <p>@($"{father.LastName} {father.FirstName}")</p>
                        </div>
                    }

                    @if (mother != null)
                    {
                        <div class="detail-group">
                            <label>Há» tÃªn máº¹</label>
                            <p>@($"{mother.LastName} {mother.FirstName}")</p>
                        </div>
                    }

                    <div class="detail-group full-width">
                        <label>Ghi chÃº</label>
                        <p class="text-muted">ChÆ°a cÃ³ ghi chÃº nÃ o cho há»c sinh nÃ y.</p>
                    </div>
                </div>
            </div>

            <div class="tab-content" id="parents">
    @if (Model.ParentStudents == null || !Model.ParentStudents.Any())
    {
        <div class="empty-state">
            <i class="fa-solid fa-users"></i>
            <p>ChÆ°a cÃ³ phá»¥ huynh Ä‘Æ°á»£c liÃªn káº¿t</p>
        </div>
    }
    else
    {
        <div class="detail-grid">
            @foreach (var link in Model.ParentStudents)
            {
                <div class="detail-group">
                    <label>@(string.IsNullOrWhiteSpace(link.Relationship) ? "Phá»¥ huynh" : link.Relationship)</label>
                    <p>@($"{link.Parent.LastName} {link.Parent.FirstName}".Trim())</p>
                </div>
            }
        </div>
    }
</div>
<div class="tab-content" id="history">
                <div class="empty-state">
                    <i class="fa-solid fa-clock-rotate-left"></i>
                    <p>ChÆ°a cÃ³ dá»¯ liá»‡u lá»‹ch sá»­ há»c táº­p</p>
                </div>
            </div>

            <div class="tab-content" id="finance">
                <div style="display:flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
                    <h4 style="margin:0;">Cáº¥u hÃ¬nh cÃ¡c khoáº£n thu riÃªng cho há»c sinh</h4>
                    <button class="btn btn-primary btn-sm" onclick="showAddFeeConfigModal()">
                        <i class="fa-solid fa-plus"></i> ÄÄƒng kÃ½ khoáº£n thu
                    </button>
                </div>
                
                <div class="table-container">
                    <table id="studentFinanceTable">
                        <thead>
                            <tr>
                                <th>Khoáº£n thu</th>
                                <th>Sá»‘ tiá»n (VNÄ)</th>
                                <th>Giáº£m trá»«</th>
                                <th>ThÃ nh tiá»n</th>
                                <th class="text-end">HÃ nh Ä‘á»™ng</th>
                            </tr>
                        </thead>
                        <tbody id="studentFinanceBody">
                            <tr><td colspan="5" class="text-center">Äang táº£i dá»¯ liá»‡u...</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal ÄÄƒng kÃ½ khoáº£n thu cho há»c sinh -->
<div class="modal-overlay" id="financeModalOverlay">
    <div class="modal-card" style="width: 500px; max-width: 95vw;">
        <div class="modal-header">
            <div class="modal-title" id="financeModalTitle">ÄÄƒng kÃ½ khoáº£n thu</div>
            <button class="modal-close" onclick="closeFinanceModal()"><i class="fa-solid fa-xmark"></i></button>
        </div>
        <div>
            <form id="studentFeeForm">
                <input type="hidden" id="studentIdHidden" value="@Model.Id" />
                <input type="hidden" id="configId" />
                
                <div class="form-group" id="feeItemSelectGroup">
                    <label class="form-label">Chá»n khoáº£n thu <span class="text-danger">*</span></label>
                    <select id="feeItemIdSelect" class="form-select" required onchange="onFeeItemChanged()">
                        <option value="">-- Chá»n khoáº£n thu --</option>
                    </select>
                </div>
                
                <div class="form-group">
                    <label class="form-label">Sá»‘ tiá»n (VNÄ) <span class="text-muted">(Äá»ƒ trá»‘ng náº¿u dÃ¹ng giÃ¡ máº·c Ä‘á»‹nh)</span></label>
                    <input type="number" id="configAmount" class="form-input" placeholder="VD: 500000">
                </div>
                
                <div style="display:grid; grid-template-columns: 1fr 1fr; gap:16px;">
                    <div class="form-group">
                        <label class="form-label">Giáº£m trá»« (Sá»‘ tiá»n)</label>
                        <input type="number" id="configDiscountAmount" class="form-input" value="0">
                    </div>
                    <div class="form-group">
                        <label class="form-label">Giáº£m trá»« (%)</label>
                        <input type="number" id="configDiscountPercentage" class="form-input" value="0" min="0" max="100">
                    </div>
                </div>
                
                <div class="form-group">
                    <label class="form-label">Ghi chÃº</label>
                    <textarea id="configNote" class="form-input" rows="2"></textarea>
                </div>
            </form>
        </div>
        <div class="d-flex justify-end mt-4 gap-2">
            <button type="button" class="btn btn-outline" onclick="closeFinanceModal()">Há»§y</button>
            <button type="button" class="btn btn-primary" onclick="saveStudentFeeConfig()">LÆ°u cáº¥u hÃ¬nh</button>
        </div>
    </div>
</div>

<script src="~/js/pages/student-finance.js" asp-append-version="true"></script>

<style>
    .student-profile-grid {
        display: grid;
        grid-template-columns: 320px 1fr;
        gap: 24px;
        margin-top: 24px;
    }

    .profile-card {
        text-align: center;
        padding: 40px 24px;
    }

    .avatar-wrapper {
        position: relative;
        width: 120px;
        height: 120px;
        margin: 0 auto 20px;
    }

    .avatar-wrapper img {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid var(--bg-main);
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }

    .status-indicator {
        position: absolute;
        bottom: 5px;
        right: 5px;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        border: 3px solid white;
    }

    .status-indicator.active { background: #10b981; }

    .student-name {
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 4px;
    }

    .student-code {
        display: inline-block;
        background: var(--primary-soft);
        color: var(--primary);
        padding: 4px 12px;
        border-radius: 20px;
        font-weight: 600;
        font-size: 0.85rem;
        margin-bottom: 24px;
    }

    .quick-stats {
        display: grid;
        grid-template-columns: 1fr 1fr;
        border-top: 1px solid var(--border);
        padding-top: 20px;
    }

    .stat-item .stat-label {
        display: block;
        font-size: 0.75rem;
        color: var(--text-muted);
        text-transform: uppercase;
        margin-bottom: 4px;
    }

    .stat-item .stat-value {
        font-weight: 600;
        color: var(--text-main);
    }

    .info-card h4 {
        margin-bottom: 20px;
        font-size: 1rem;
    }

    .info-item {
        display: flex;
        gap: 16px;
        margin-bottom: 20px;
    }

    .info-item i {
        color: var(--primary);
        font-size: 1.1rem;
        margin-top: 4px;
    }

    .info-item label {
        display: block;
        font-size: 0.75rem;
        color: var(--text-muted);
        margin-bottom: 2px;
    }

    .info-item p {
        margin: 0;
        font-weight: 500;
    }

    /* Main Card Styling */
    .card-header-tabs {
        display: flex;
        gap: 32px;
        border-bottom: 1px solid var(--border);
        margin-bottom: 30px;
        padding: 0 24px;
    }

    .tab-btn {
        background: none;
        border: none;
        padding: 16px 0;
        font-weight: 600;
        color: var(--text-muted);
        cursor: pointer;
        position: relative;
        transition: all 0.2s;
    }

    .tab-btn.active {
        color: var(--primary);
    }

    .tab-btn.active::after {
        content: '';
        position: absolute;
        bottom: -1px;
        left: 0;
        width: 100%;
        height: 2px;
        background: var(--primary);
    }

    .tab-content {
        display: none;
        padding: 0 24px 30px;
    }

    .tab-content.active {
        display: block;
    }

    .detail-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 24px;
    }

    .detail-group label {
        display: block;
        font-size: 0.85rem;
        color: var(--text-muted);
        margin-bottom: 8px;
    }

    .detail-group p {
        background: var(--bg-main);
        padding: 12px 16px;
        border-radius: 8px;
        font-weight: 500;
        margin: 0;
    }

    .detail-group.full-width {
        grid-column: span 2;
    }

    .empty-state {
        text-align: center;
        padding: 60px 0;
        color: var(--text-muted);
    }

    .empty-state i {
        font-size: 3rem;
        margin-bottom: 16px;
        opacity: 0.3;
    }
</style>

<script>
    document.querySelectorAll('.tab-btn').forEach(btn => {
        btn.addEventListener('click', () => {
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
            document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
            
            btn.classList.add('active');
            document.getElementById(btn.dataset.tab).classList.add('active');
        });
    });
</script>
